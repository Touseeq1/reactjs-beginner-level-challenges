import React, { useEffect, useLayoutEffect } from 'react'
const Covid1=()=> {
    useEffect(()=>{
      fetch("api")})
    return (
      <><h1>Live</h1>
      <h2>Covid-19-Corona-Traker</h2></>);}
  export default Covid1;
// render:to display the specified HTML code inside the specified HTML element.